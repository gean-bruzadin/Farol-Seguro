@model Farol_Seguro.Models.Cidade

@{
    ViewData["Title"] = "Criar Cidade";
}

<link href="~/css/header.css" rel="stylesheet" />
<link href="~/css/Form.css" rel="stylesheet" />

<div class="form-container">
    <div class="form-section">
        <h1 class="form-title">Cadastrar Nova Cidade</h1>

        @* Exibição de Mensagem de Erro *@
        @if (TempData["MensagemErro"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong>Erro!</strong> @TempData["MensagemErro"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <form asp-action="Criar" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="form-group">
                <label asp-for="Nome_Cidade" class="form-label">Nome da Cidade</label>
                <input asp-for="Nome_Cidade" class="form-control" required />
                <span asp-validation-for="Nome_Cidade" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Id_Estado" class="form-label">Estado</label>
                <select asp-for="Id_Estado" class="form-control" asp-items="ViewBag.Id_Estado" required>
                    <option value="">Selecione um estado</option>
                </select>
                <span asp-validation-for="Id_Estado" class="text-danger"></span>
            </div>

            <div class="btn-group btn-group-center">
                <button type="submit" class="btn btn-primary">Cadastrar</button>
                <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<script src="~/js/menu_dropdown.js"></script>
<script src="~/js/open-modal.js"></script>