@model Farol_Seguro.Models.Funcionario

@{
    ViewData["Title"] = "Editar Funcionário";
}

<link href="~/css/header.css" rel="stylesheet" />
<link href="~/css/Form.css" rel="stylesheet" />

<div class="form-container">
    <div class="form-section">
        <h1 class="form-title">Editar Funcionário</h1>

        @if (TempData["MensagemSucesso"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <strong>Sucesso!</strong> @TempData["MensagemSucesso"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        @if (TempData["MensagemErro"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong>Erro!</strong> @TempData["MensagemErro"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <form asp-action="Editar" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id_Funcionario" />

            <div class="form-group">
                <label asp-for="Nome_Funcionario" class="form-label">Nome do Funcionário</label>
                <input asp-for="Nome_Funcionario" class="form-control" />
                <span asp-validation-for="Nome_Funcionario" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Email_Funcionario" class="form-label">E-mail</label>
                <input asp-for="Email_Funcionario" class="form-control" type="email" />
                <span asp-validation-for="Email_Funcionario" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Senha_Funcionario" class="form-label">Senha</label>
                <input asp-for="Senha_Funcionario" class="form-control" type="password" value="" placeholder="Deixe em branco para não alterar" />
                <span asp-validation-for="Senha_Funcionario" class="text-danger"></span>
                <div class="form-help">Deixe em branco para manter a senha atual</div>
            </div>

            <div class="form-row">
                <div class="form-col">
                    <div class="form-group">
                        <label asp-for="Departamento_Funcionario" class="form-label">Departamento</label>
                        <input asp-for="Departamento_Funcionario" class="form-control" />
                        <span asp-validation-for="Departamento_Funcionario" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-col">
                    <div class="form-group">
                        <label class="form-label">Cargo</label>
                        <select asp-for="Id_Cargo" asp-items="ViewBag.Id_Cargo" class="form-control"></select>
                        <span asp-validation-for="Id_Cargo" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="btn-group btn-group-center">
                <button type="submit" class="btn btn-primary">Salvar Alterações</button>
                <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}